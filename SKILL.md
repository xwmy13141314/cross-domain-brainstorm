---
name: cross-domain-brainstorm
description: |
  当用户需要为产品、功能、交互或服务模式生成新创意，且当前思路被行业惯性、同质化方案或脑暴卡壳困住时使用。
  核心方法是“锁定问题 → 抽取跨域领域 → 提炼底层机制 → 强制嫁接 → 快速收敛”，通过跨域机制迁移生成一批候选创意并筛出最值得推进的方向。
  适用于 brainstorm、功能创新、交互破局、模式创新和团队共创；不用于行业研究、竞品分析、根因诊断、PRD 定稿或文章写作。
---

# 跨域迁移脑暴

将本 skill 当成 **创意生成器** 使用。

目标不是先分析清楚，而是先打破定势、生成变化、炸出新点子，再筛出最值得推进的方向。

## 什么时候用

在以下场景触发：

- 需要 brainstorm / 脑暴
- 需要产品创新、功能创新、交互创新、服务模式创新
- 现有方案过于同质化，明显被行业惯性锁死
- 希望借用其他行业、其他产品、其他系统的底层机制生成新想法
- 团队共创时缺少新刺激，需要快速打开思路
- 用户明确提到“跨界”“跨域”“迁移”“借别的行业机制”“炸点子”“打破思维定势”

高匹配表达包括但不限于：

- 帮忙跨界脑暴一下
- 帮忙用别的行业机制想新功能
- 当前脑暴卡住了，帮忙炸点新想法
- 想跳出行业内卷，借别的领域机制看看
- 帮忙想一些不那么像同行的方案
- 帮忙做功能创新 / 交互创新 / 模式创新

## 不要用在这里

不要用于：

- 行业研究、竞品研究、公司研究、技术研究
- 根因分析、问题诊断、复盘
- 判断“哪个方案最好”
- 写 PRD、研究报告、文章
- 用户已经有明确方案，只需优化细节
- 用户只想要 1 个直接建议，而不是多个创意候选

遇到这些情况时，优先切换：
- 深度研究 → `hv-analysis`
- 产品判断 → `product-thinking`
- 长文写作 → `khazix-writer`

## 核心原则

始终遵守以下原则：

- 迁移 **底层机制**，不要照搬 **表面功能**
- 发散阶段不否定、不提前讨论可行性
- 收敛阶段再引入理性判断
- 先追求候选数量和多样性，再追求最优解
- 如果结果只是“把别人的界面/按钮/玩法搬过来”，判定为低质量创意

## 最小工作流

### Step 1：锁定问题

先明确三件事：

- 目标对象是什么
- 最核心的一个痛点是什么
- 本次更想要“创意池”还是“Top 3 方向”

如果输入不清楚，先补问，不直接发散。

优先补问：
- 当前要解决的对象是什么？
- 最核心的一个痛点是什么？
- 这次更想先多出点子，还是直接筛重点？

### Step 2：抽取 3 个跨域领域

默认抽 3 个异质领域：

- 1 个传统行业
- 1 个数字 / AI 领域
- 1 个服务 / 流程领域

如果用户已指定领域，则优先使用用户指定，再补 1-2 个反差较大的领域。

需要领域库时，读取：
- `references/domain-library.md`

### Step 3：提炼底层机制

针对每个领域，只回答一句：

> 这个领域靠什么机制，解决了什么问题？

只提炼“规则 / 反馈 / 流程 / 约束 / 运作方式”，不要分析具体功能、页面和外观。

如果需要正反例和机制示例，读取：
- `references/mechanism-examples.md`

### Step 4：强制嫁接并发散

对每个领域至少生成 2 个候选创意，统一使用这个句式：

> 如果把【该领域】的【底层机制】搬到【目标对象】里，会变成什么功能、交互或模式？

默认至少产出 6 个候选点子。

然后对最有潜力的方向继续补 2-3 个变体，优先做：
- 简化版
- 反向版
- AI 增强版 或 场景限定版

### Step 5：快速收敛

从所有候选中筛出 Top 3，按 4 个维度快速评估：

- 痛点匹配度
- 技术可行性
- 差异化
- 用户接受度

不需要复杂打分模型，但必须明确：
- 哪些是高优先级
- 哪些只是有趣但不值得先做
- 哪个方向最值得先验证

## 默认输出格式

默认按以下顺序输出：

### 1. 问题定义
- 目标对象
- 核心痛点
- 本次输出目标

### 2. 抽取的跨域领域
- 领域 A
- 领域 B
- 领域 C

### 3. 每个领域的底层机制
按“领域 → 机制 → 解决的问题”输出。

### 4. 创意池
至少给出 6 个候选创意。
每个创意用一句话说清：
- 借了什么机制
- 变成了什么功能 / 交互 / 模式

### 5. Top 3 创意
对每个 Top 3 补充：
- 创意名称
- 来源领域
- 核心机制
- 为什么值得优先推进
- 适配建议

### 6. 下一步验证建议
给出最轻量的验证方式，例如：
- 用户访谈
- 低保真草图
- 单点原型
- 子场景灰度尝试

需要模板时，读取：
- `references/output-templates.md`

## 使用模式

### 单人模式
当用户是个人使用时，直接代替主持人和参与者完成整套流程。

### 团队模式
当用户明确在开会、共创、团队脑暴时，用主持人口吻分轮次引导推进。

## 失败兜底

如果输出结果过于平庸，按以下顺序修正：

1. 重抽领域，优先增加反差更大的领域
2. 重新下探机制层，不要停留在表面功能
3. 强制补一个反向版或场景限定版
4. 如果仍然无效，明确说明：
   - 当前问题定义过大
   - 或核心痛点不够聚焦
   - 需要先收窄问题，再继续脑暴

## 参考资料

按需读取：

- `references/domain-library.md`
- `references/mechanism-examples.md`
- `references/output-templates.md`
- `references/example-session.md`

## 一句话提醒

这个 skill 的任务不是“研究清楚”，而是先 **打破定势，制造变化，生成新点子**。

先把创意炸出来，再决定哪些值得做。
